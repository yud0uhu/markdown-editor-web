{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "wasm": "wasm-pack build ./markdown-perser --target web"
  },
  "devDependencies": {
    "nuxt": "^3.2.2",
    "vite": "^4.1.4",
    "vite-plugin-wasm-pack": "^0.1.12"
  }
}
